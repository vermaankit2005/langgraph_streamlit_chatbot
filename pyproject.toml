[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "comedybot"
version = "0.1.0"
description = "LangGraph + Streamlit comedian chatbot"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["backend"]
